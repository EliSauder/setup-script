#!/bin/bash

source "$SCRIPT_DIRECTORY/sys_scripts/logout"

find "$SCRIPT_DIRECTORY/user_scripts/" -name "*zsh_logout_pre" -exec source {} \;

find "$SCRIPT_DIRECTORY/user_scripts/" -name "*zsh_logout" -exec source {} \;

find "$SCRIPT_DIRECTORY/user_scripts/" -name "*zsh_logout_post" -exec source {} \;
