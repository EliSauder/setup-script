#!/bin/bash

source "$SCRIPT_DIRECTORY/sys_scripts/env"

find "$SCRIPT_DIRECTORY/user_scripts/" -name "*zsh_env_pre" -exec source {} \;

find "$SCRIPT_DIRECTORY/user_scripts/" -name "*zsh_env" -exec source {} \;

find "$SCRIPT_DIRECTORY/user_scripts/" -name "*zsh_env_post" -exec source {} \;
